import React, { useEffect, useState } from "react";
import "./App.css";

import Login from "./Auth/login";

const App = () => {
  const element = (
    <div>
      <Login></Login>
    </div>
  );

  return element;
};

export default App;
